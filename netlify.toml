[build]
  command = "npm run build"
  publish = "dist"

# /projects merged into the homepage work section (July 2026)
[[redirects]]
  from = "/projects"
  to = "/#work"
  status = 301

[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
